[project]
name = "api_gateway"
version = "0.1.0"
description = "authentication, api keys, logging, rate_limite and etc"
authors = [
    {name = "Noso4ek27",email = "ssaveliymedvedev@gmail.com"}
]
license = {text = "apache 2.0"}
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
